[package]
name = "arm-mdns"
version = "0.1.1"
edition = "2021"
keywords = ["arm", "mdns", "service", "daemon"]
categories = ["embedded"]
description = "ARM mDNS registration daemon"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
mdns-sd = { version = "0.12", default-features = false }
